#pragma once

#include <gflags/gflags.h>

DECLARE_string(content_root);
DECLARE_int32(port);
DECLARE_string(host);
DECLARE_string(novel_name);
DECLARE_string(hook_config);
DECLARE_string(auth_username);
DECLARE_string(auth_password);
DECLARE_int32(auth_timestamp_tolerance_ms);
